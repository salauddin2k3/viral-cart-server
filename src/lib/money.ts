export function roundHalfUp(value: number): number {  return Math.round((value + Number.EPSILON) * 100) / 100;}export function calculateFinalPrice(regularPrice: number, discountPercent: number): number {  if (discountPercent <= 0 || discountPercent >= 100) {    return roundHalfUp(regularPrice);  }  return roundHalfUp(regularPrice * (1 - discountPercent / 100));}export function formatTaka(amount: number): string {  const formatted = amount.toFixed(2).replace(/\B(?=(\d{3})+(?!\d))/g, ',');  return `\u09F3${formatted}`;}export function parseMoneyInput(input: string): number {  const cleaned = input.replace(/[aº¦,\s]/g, '');  const num = Number(cleaned);  if (!Number.isFinite(num) || num < 0) {    throw new Error('Invalid money value');  }  return roundHalfUp(num);}